name: Deployment
on:
  push:
    branches:
      - main
      - dev
env:
  # mongodb+srv://admin:_njDhdLp.9SXFmP@cluster0.0rwnqds.mongodb.net/
  MONGODB_BD_NAME: github_actions
  MONGODB_CLUSTER_ADDRESS: cluster0.0rwnqds.mongodb.net
  MONGODB_USERNAME: admin
  MONGODB_PASSWORD: _njDhdLp.9SXFmP
  PORT: 3000
jobs:
  test:
    # environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3

      - name: Cache dependencies
        uses: actions/cache@v3
        
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}

      - name: Install dependencies
        run: npm ci
      
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT

      - name: Run tests
        run: npm test

      - name: Output information
        run: |
          echo "MONGODB_CLUSTER: ${{env.MONGODB_CLUSTER_ADDRESS}}"
          echo "MONGODB_USERNAME: $MONGODB_USERNAME"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "..."
